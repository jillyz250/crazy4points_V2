/**
 * Site-wide constants.
 *
 * SITE_URL is the canonical public origin. Use this for:
 * - canonical tags / OpenGraph / JSON-LD URLs
 * - sitemap + robots.txt + llms.txt entries
 * - links inside transactional emails
 *
 * The apex `crazy4points.com` 301-redirects to www on Vercel, so the
 * canonical form is www. If that ever changes, edit this one line.
 */
export const SITE_URL = 'https://www.crazy4points.com'
