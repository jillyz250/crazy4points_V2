# Handoff — 2026-05-02

Thread context: this conversation got long and started misbehaving (token bloat, repeated injection of irrelevant Vercel skill suggestions). Continuing in a fresh thread.

---

## Where I left off

**About to start Atmos Rewards** (loyalty_program type, covers Alaska + Hawaiian carriers). Researched nothing yet — was just finishing the skill update for the new WebSearch-default research workflow.

User said "lets do atmos next" → I confirmed Atmos was the target, started updating the skill to use WebSearch instead of forcing user to paste 6 URLs. Skill update is **complete and saved**:
- `.claude/skills/add-airline/SKILL.md` Step 1 rewritten
- `memory/feedback_websearch_default_research.md` saved
- `memory/MEMORY.md` index updated

Then user hit API errors and asked for this handoff.

## Resume next thread with

> "let's do atmos next"

The skill will now do WebSearch research directly (TPG / OMAAT / Frequent Miler / AwardWallet 2026-dated) and only ping the user with specific 1-question Google asks if WebSearch can't pin something down.

---

## Today's session shipped (2026-05-02)

| | |
|---|---|
| **New rule** | WebSearch is default research; user-paste is fallback. `feedback_websearch_default_research.md` saved. SKILL.md Step 1 rewritten. The 6-URL paste-in pattern is **retired**. |
| **Lounge day-pass rule refined** | Per-carrier research for fare-class exclusion (Delta = "Main Basic", United = "Basic Economy", Alaska = "Saver", BA = "Basic"). Don't write boilerplate. |
| **Backlog cleanup** | Alaska + Delta + United `lounge_access` fields enriched with day-pass entry rules (4 facts: same-day BP, time window, fare-class exclusion, scope). |

## Yesterday's session (2026-05-01) — context for next thread

| | |
|---|---|
| **2 new program pages** | Delta + United (both fully authored, indexed, source docs written, RSS in Scout) |
| **PRs merged** | #273 (alliance-context for writer + fact-checker), #279 (prose table styling), #280 (alliance card tier crossover), #281 (alliance card CTA), #282 (By Status form client component), #283 (equivalent-status cards), #284 (alliance explorer pitch tightening), #285 (bullet-list card styling) |
| **Skill changes** | Step 0 dropped (workflow now opens at Step 1); transfer tax/fee rule baked in; max-6 verified URLs (now superseded by WebSearch-first); combined preview before paste blocks |

## US airline progress

3 of 12 done: **Alaska, Delta, United**.

Next options:
1. **Atmos Rewards** — loyalty program covering Alaska + Hawaiian. (User just picked this — start here.)
2. **American Airlines** — oneworld founder
3. **Hawaiian** — closes the Alaska/Hawaiian carrier-vs-loyalty trio with Atmos
4. **Southwest, JetBlue, Spirit, Frontier, Allegiant, Avelo, Breeze, Sun Country**

## Open backlog

- **Cross-page integration** for writer/fact-checker — already shipped in PR #273
- **5-airline trigger** for Resources nav dropdown — still 2 away
- **Hyatt 2026 hotel category transition** — older memory note
- **Tier 1 credit card pages** — paused after airlines

## Key memory rules to know (for the next thread)

1. **WebSearch first, paste-in fallback** (`feedback_websearch_default_research.md`)
2. **Combined preview before per-field paste blocks** (`feedback_combined_preview_before_paste.md`)
3. **Lounge day pass = 4 facts, fare-class researched per-carrier** (`feedback_lounge_day_pass_rules.md`)
4. **Capture transfer taxes/fees in transfer_partners notes** (`feedback_capture_transfer_fees.md`)
5. **Always real URLs, no `?nocache=1`** (`feedback_real_urls_only.md`)
6. **ASCII-only in SQL data** (`feedback_ascii_only_in_sql_data.md`)
7. **Brand voice: sassy traveler-friend** (`feedback_brand_voice_sassy.md`)

## Unfinished thread mess to ignore

The current thread has been getting flooded with auto-injected Vercel skill suggestions (chat-sdk, ai-sdk, auth, deployments-cicd, etc.) that have nothing to do with the work. Ignore them — the project doesn't use those Vercel skills directly. They're hook noise from the user's session-start config.
